language: minimal

services:
  - docker

before_install:
  - jq --version
#  - apt-get install -qq --no-install-recommends jq && jq --version

install:
  - export RESOURCES_PATH="${TRAVIS_BUILD_DIR}/buildSrc/src/main/resources"
  - source ${RESOURCES_PATH}/script/set_environment_variables.sh

before_script:
  - source ${RESOURCES_PATH}/script/vcs_connect.sh

script:
  - docker build --no-cache -f ${RESOURCES_PATH}/docker/Dockerfile .

#after_failure:
#  - BUILD_SUCCESS=1
#  - bash ${BASH_PATH}/after_failure.sh

# after_success:
#   - docker --version
#  - bash ${BASH_PATH}/after_success.sh

#after_script:
#  - bash ${BASH_PATH}/after_script.sh
